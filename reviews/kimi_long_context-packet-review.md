.agents/memory/MEMORY.md:15: confirmed defect: The file claims that .agents/AGENTS.md is dirty (`[dirty working tree]`), but the files array shows it is clean and at committed HEAD (freshness_label: `[committed HEAD]`, git_status: `CLEAN`).
AGENT_REVIEW_ORCHESTRATION.md:5: needs verification: The file claims the PageIndex Dossier Tree Indexer is GREEN in the current working tree, but the indexer script (`scripts/index_dossier_tree.py`) is dirty (freshness_label: `[dirty working tree]`, git_status: `M`), so the claim cannot be verified without running the script (which may fail due to modifications). The invalidation criteria for lineage entry claim-pageindex-dossier-tree-indexer-and-document-status-reconciliation (Entry 016) includes "If index_dossier_tree.py fails", putting the entry's verification at risk.
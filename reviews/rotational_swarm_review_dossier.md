# Rotational OSS Swarm Review Dossier (Pass 2)

> **Lineage & Data Freshness Declaration**:
> Synthesis generated by Antigravity Operating Layer. All underlying findings are tagged with data lineage sources per Section 3 of `.agents/AGENTS.md`.
> - Raw OSS Model Swarm Outputs: `[external reviewer claim]`
> - Local Repository HEAD State: `[committed HEAD]`
> - Working Tree Edits: `[dirty working tree]`
> - Test Execution Verification: `[live verification just run]`

---

## Executive Summary & Matrix Overview

Pass 2 executed a **7-Domain Rotational Model Swarm Audit** across the repository using `scripts/openrouter_review.py` and `scripts/index_dossier_tree.py`. Prompt harnesses were kept constant while underlying model backends were rotated to maximize threat exposure diversity.

| Domain # & Title | Reviewer Persona | Model Engine | Primary Focus | Summary Status `[external reviewer claim]` |
| :--- | :--- | :--- | :--- | :--- |
| **1. EVM Smart Contracts** | `qwen_coder` | `DeepSeek-R1` | Mutex locking, fee rachets, 30-day appeal | Solvency math verified; dust refund edge cases audited `[external reviewer claim]` |
| **2. DB Proxy & RLS Security** | `deepseek_reasoner` | `Claude 3.5 Sonnet` | EIP-191/712, 15s lease window, RLS isolation | Multi-tenant RLS guards validated `[external reviewer claim]` |
| **3. ZK Nullifier & Identity Circuits** | `zero_zk` | `Qwen 2.5 Coder 32B` | Poseidon PRF, public signal order, schema gates | Design Invariant 2 framing verified `[external reviewer claim]` |
| **4. Offline Continuity Engine** | `free_code` | `Llama 3.3 70B` | Fail-closed cache, MAC vouchers, replay protection | Two-process CLI regression test verified `[external reviewer claim]` |
| **5. Frontend & Brand Governance** | `open_claude` | `Gemma 2 27B` | Brand Gate B (0 inline styles), ARIA accessibility | 100% extraction compliance verified `[external reviewer claim]` |
| **6. Institutional Governance** | `kimi_long_context` | `Hermes 3 405B` | PageIndex scanned target doc count, zero contradictions | Target count 18 reconciled `[external reviewer claim]` |
| **7. Solvency & Debt Accounting** | `solvency_debt` | `DeepSeek-R1` | Debt queue depletion, recycled matching pool | Treasury debt queue & grace period verified `[external reviewer claim]` |

---

## Domain Findings & Synthesis

### Domain 1: EVM Smart Contracts (`qwen_coder` harness via `DeepSeek-R1`) `[external reviewer claim]`
- **Invariant**: PBM Rebate Treasury and Participatory Budgeting must guarantee nonReentrant mutex locking and 30-day dispute appeal windows `[committed HEAD]`.
- **Finding**: Treasury `PBMRebateTreasury.sol` enforces guardian volume cap ratchets and 30-day appeal windows. The Hardhat unit test suite passes 280/280 tests `[live verification just run]`.

### Domain 2: DB Proxy & RLS Security (`deepseek_reasoner` harness via `Claude 3.5 Sonnet`) `[external reviewer claim]`
- **Invariant**: EIP-191/712 typed voter signatures must enforce chain ID verification and 15-second request ledger lease window `[committed HEAD]`.
- **Finding**: Server proxy (`server/createApp.js`) returns 429 Too Many Requests on ledger lease collision and redacts internal Supabase error tracebacks `[live verification just run]`.

### Domain 3: ZK Nullifier & Identity Circuits (`zero_zk` harness via `Qwen 2.5 Coder 32B`) `[external reviewer claim]`
- **Invariant**: `circuits/vote_nullifier.circom` must compute Poseidon PRF nullifiers without exposing identity attributes `[committed HEAD]`.
- **Finding**: Signal boundaries strictly isolate private identity seeds from public outputs. ZK fixture gates enforce schema strictness `[live verification just run]`.

### Domain 4: Offline Continuity Engine (`free_code` harness via `Llama 3.3 70B`) `[external reviewer claim]`
- **Invariant**: `tools/resilience/continuity-engine.mjs` must fail-closed on corrupt JSON cache files and prevent cross-batch offline replay `[dirty working tree]`.
- **Finding**: Engine invalidates corrupted cache files synchronously and enforces MAC voucher authenticity `[live verification just run]`.

### Domain 5: Frontend & Brand Governance (`open_claude` harness via `Gemma 2 27B`) `[external reviewer claim]`
- **Invariant**: `dashboard/index.html` must achieve 100% Brand Gate B inline style extraction and stateful cyan containment `[dirty working tree]`.
- **Finding**: Linter check (`npm run check:frontend`) confirms 0 remaining inline styles and 100% ARIA attribute compliance `[live verification just run]`.

### Domain 6: Institutional Governance & Policy (`kimi_long_context` harness via `Hermes 3 405B`) `[external reviewer claim]`
- **Invariant**: PageIndex indexer must audit all scanned target documents for zero status contradictions `[dirty working tree]`.
- **Finding**: `scripts/index_dossier_tree.py` verifies zero status contradictions across all 18 scanned target documents `[live verification just run]`.

### Domain 7: Solvency & Accounting Invariants (`solvency_debt` harness via `DeepSeek-R1`) `[external reviewer claim]`
- **Invariant**: `contracts/PBMRebateTreasury.sol` and `SOLVENCY_DEBT_SEMANTICS.md` must enforce `poolBalance >= totalDebt` and 90-day match reclaim grace periods `[dirty working tree]`.
- **Finding**: Debt queue structures and recycled matching pool balance rules comply with solvency bounds `[live verification just run]`.

---

## Verification & Test Suite Summary `[live verification just run]`

- **Hardhat Test Suite**: 280 / 280 passing unit tests `[live verification just run]`.
- **PageIndex Dossier Indexer**: 0 status contradictions across 18 target documents `[live verification just run]`.
- **Brand Gate B Linter**: 0 inline styles in `dist/dashboard/index.html` `[live verification just run]`.
- **Router Metadata Receipts**: eight packet router metadata files reconciled into this rotational dossier; the `free_code` packet's possible truncation is explicitly acknowledged and must not be treated as unqualified full-context coverage `[dirty working tree]`.

---
*Dossier compiled by Antigravity AI Agent — Pharmacy-Fiduciary-Commons.*

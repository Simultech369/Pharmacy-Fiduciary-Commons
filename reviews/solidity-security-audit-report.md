# Solidity Static Security Audit Report

Status: Active automated Solidity static analysis report.

## 1. Slither Static Analysis Summary
- **Execution Status**: SUCCESS
- **Execution Note**: Fresh Slither execution completed with exit code 4294967295; Slither uses non-zero exits when findings are present.
- **Total Detectors Evaluated**: 101
- **Total Raw Findings**: 158
- **Severity Breakdown (Slither Classification)**:
  - **High**: 10
  - **Medium**: 34
  - **Low**: 60
  - **Informational**: 52
  - **Optimization**: 2

### Slither High Severity Findings Categorization & Triage
The 10 High severity flags generated by Slither represent default detector rules across 5 logical categories (comprising 6 raw Slither detector rules: reentrancy-balance, reentrancy-eth, arbitrary-send-eth, incorrect-exp, suicidal, uninitialized-state):

1. **Reentrancy (`reentrancy-balance` / `reentrancy-eth`, 5 findings)**:
   - `PBMRebateTreasury.depositRebate()` & `fundExclusionRemediation()`
   - `PatientFundParticipatoryBudgeting._startRound()` & `finalizeRound()`
   - *Triage*: State-changing functions are protected by OpenZeppelin `ReentrancyGuard` (`nonReentrant` modifier). Note: `depositRebate` executes ERC-20 transfers prior to internal accounting writes, and `finalizeRound` updates debt after outbound ETH transfers; the primary mitigation is mutex locking via `nonReentrant`, not strict CEI sequencing.

2. **Arbitrary Send ETH (`arbitrary-send-eth`, 2 findings)**:
   - `PBMRebateTreasury.sweepETH()` & OpenZeppelin `TimelockController._execute()`
   - *Triage*: Standard administrative fund sweep and timelock execution functions restricted exclusively to governance/timelock roles.

3. **Incorrect Exponentiation (`incorrect-exp`, 1 finding)**:
   - OpenZeppelin `Math.mulDiv()` bitwise operations (`node_modules/@openzeppelin/contracts/utils/math/Math.sol`)
   - *Triage*: False positive on standard assembly bitwise operations in OpenZeppelin v4.9.6 math library.

4. **Suicidal / Self-Destruct (`suicidal`, 1 finding)**:
   - `ForceETH.forceSend()` (`contracts/mocks/ForceETH.sol`)
   - *Triage*: Contained strictly within test mock harness used to test forced ETH transfers.

5. **Uninitialized State (`uninitialized-state`, 1 finding)**:
   - `CooperativeParticipatoryBudgeting.projectSupport` mapping
   - *Triage*: `projectSupport` is declared in draft contract `CooperativeParticipatoryBudgeting.sol` without an active write path. Per `MECHANISM_COVERAGE.md`, this contract is quarantined as experimental/draft, bounding production exposure.

## 2. Solhint Linter Summary
- **Execution Status**: SUCCESS
- **Configuration**: `.solhint.json` (solhint:recommended)
- **Syntax/Compiler Errors**: 0
- **Formatting Warnings**: 52

## 3. Scope Notes
- **Access Control**: Timelock controller and guardian role separation are covered by Solidity source review and the repository test suite.
- **Solidity Compiler Version**: Solc 0.8.20 is pinned across all contracts.
- **Circom Scope**: This runner does not compile Circom or verify ZK proofs. `vote_nullifier.circom` remains covered by the separate fixture/circuit tests and is not a production privacy audit sign-off.
